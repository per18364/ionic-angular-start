import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemyCourse.ionicAngular',
  appName: 'ionic-angular-start',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
